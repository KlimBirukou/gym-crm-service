spring:
  datasource:
    url: jdbc:postgresql://gym-db:5432/gym_db?currentSchema=gym_schema
    username: gym_user
    password: vG8k2Pz9mN4Xw7Tq


  liquibase:
    contexts: default

management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
    prometheus:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

logging:
  level:
    root: WARN
    com.epam.gym: INFO
    org.hibernate.SQL: OFF
    org.hibernate.type.descriptor.sql.BasicBinder: OFF

application:
  auth:
    secret: cHJvZC1zZWNyZXQta2V5LWZvci1neW0tY3JtLWFwcC1wcm9kdWN0aW9uLW9ubHk=
    jwt-expiration: 28800
